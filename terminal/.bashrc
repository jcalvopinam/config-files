#----------------------------------------------------------------------------------
# Load custom configuration
#----------------------------------------------------------------------------------
[[ -f ~/.exports ]] && . ~/.exports
[[ -f ~/.aliases ]] && . ~/.aliases
[[ -f ~/.functions ]] && . ~/.functions
[[ -f ~/.keybindings ]] && . ~/.keybindings
[[ -f ~/.docker-commands ]] && . ~/.docker-commands
[[ -f ~/.work ]] && . ~/.work

#----------------------------------------------------------------------------------
# autoload
#----------------------------------------------------------------------------------
themeToggle
