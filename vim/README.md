# Basic configuration for vim and nvim

## vim
```shell
ln -s ~/space/config-files/vim/.vimrc ~/
```

## nvim
```shell
mkdir -p ~/.config/nvim
ln -s ~/space/config-files/vim/.vimrc ~/.config/nvim/init.vim
```
